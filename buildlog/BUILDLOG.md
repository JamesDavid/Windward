# Build Log: WINDWARD — Archipelago of the Gods

Genre: Tower Defense & Strategy

---

## Decisions locked so far

*Keep this list current. Re-read it at the start of every session. When a decision changes, edit it here AND note the change in that session's entry.*

### Scope
- Two factions only: Aeolus (player, air) and Poseidon (AI, sea). Gaia and Zeus are future state.
- One map **template** on a 12 × 20 portrait grid, nine islands, corner opposition — **deterministically generated per match from a seed** (spec §20A supersedes the earlier "one handcrafted map" decision; changed in Session 1). Free-form procedural maps remain cut; generation is zone-constrained and validated.
- Target session: ~7 minutes, nine waves.
- Single-player, portrait, Three.js, fully offline.

### Core systems
- Route pieces: hand of three, five shapes (short straight, long straight, L, S, T). Tap piece → tap socket → tap to cycle orientation → confirm. No dragging.
- Support graph: every segment must trace to an anchor. Loss of support causes progressive collapse from the exposed end inward, 3 s rescue window then one segment per 3 s.
- ~~Structures are network **nodes, not caps**~~ **REVERSED by player direction (Session 2):** defenses (vane/bolt/shield/mast) are **solid caps** — zero ports, no onward attachment, and they block transit when standing on a route; ships mid-journey through a newly sealed road go adrift. Temples remain junctions.
- **Salvage (NetStorm-style, player-directed):** own structures reclaim half cost in Supply; route segments unbind for 1 Favor each. No explosion, but support recalc runs — salvaging load-bearing pieces frays what hangs beyond.
- **Kill bounty (NetStorm-style, player-directed):** a downed craft pays Favor to the opposing side (2/3/5 by class, adrift losses included); a combat-killed structure pays ¼ its cost in Supply. Claim-crumbles and collapses pay nothing.
- Two placement contexts: network endpoints, and island plots (2 per island, 3 on a Temple).
- Influence gates all construction. Overlap is contested: buildable by both, Favor to neither.
- Islands are claimed by building a Temple. Priest must be present. Completing a Temple crumbles opposing connections from that island.
- Economy: Supply mined into a local stockpile, hauled home, credited on arrival. Islands deplete.
- Wind field: dot product with route direction sets speed. Drifts ±30°, never reverses. Air affected strongly, sea slightly.
- Ships have hull integrity, regenerate, and go **adrift** when their segment loses support.

### Constraints
- All own game code consolidated into a single readable, unminified `index.html`.
- Three.js under `/vendor`, relative paths, no CDN.
- All assets local. Audio synthesised at runtime with WebAudio (zero bytes, zero requests). **Amended (Session 2, player-directed + rules check):** rules permit rights-cleared third-party content; `/vendor` may carry three.js AND its own example modules (MIT). Textures remain generated at runtime — zero image assets.
- ZIP ≤ 35 MB, `index.html` at root.
- **No numeric literals in system logic** — all tuning values live in one frozen `CONFIG` object at the top of `index.html`.
- No identifying information anywhere: code comments, metadata, repo URLs, filenames, page title.

### Art
- Mythic Hellenic bronzepunk. Aeolus warm ivory/gold, Poseidon cold teal/green.
- Airships are converted triremes: hull → gondola, oars → fins, sail → envelope.
- Fixed oblique camera, no free rotation.

### Cut / not building
Gaia, Zeus, earth and storm layers, throughput and congestion, route and terminal repair, in-match blessings, fog on terrain, multiplayer, procedural maps, worker entities.

---

### Submission artefacts status
- **Design Intent** (`docs/design-intent.docx`): refreshed 2026-08-15 to match the built game (scrolling seeded archipelago, roads-are-reach, raw-end exposure, lane-reach waves, exploration fog). 491 words, template's seven sections, regenerated with no author metadata anywhere in the file.
- **Build Log**: this file, updated with every commit.
- **Playable build**: `index.html` + `/vendor/three.min.js`, fully offline, all art procedural, all audio WebAudio-synthesised. Contest zip will be produced only when instructed.

## Session log

<!--
ENTRY TEMPLATE — copy for each session.

## Session N (YYYY-MM-DD): <short title>

**Tool(s):** 
**Commits:** `<first>`..`<last>`

### Prompts and commits
| # | Prompt (summary) | Commit | Result |
|---|---|---|---|
| 1 |  | `abc1234` |  |

**What I built:**
**Key decisions and why:**
**Pivots, and what changed my thinking:**
**What changed after playtesting:**
**Biggest problems, and how I solved them:**
**What I learned:**
**Where things stand / next:**
-->

## Session 0 (2026-08-15): setup

**Tool(s):** —
**Commits:** —

**What I built:** Repository initialised, spec finalised, build log started.

**Key decisions and why:** Locked the decisions above before writing code, so the agent has a stable reference and stops re-litigating settled choices between sessions.

**Where things stand / next:** Day 1–2 spikes — touch placement on a real phone, priest-tap feel, portrait air-vs-sea legibility, AI route feasibility. Any red flag means simplifying before adding systems.

## Session 1 (2026-08-15): scaffold, build pipeline, deterministic map generator

**Tool(s):** Claude Code (agent wrote all code from the spec; no hand-coding)
**Commits:** `ddd8dea`..`949c777`

### Prompts and commits
| # | Prompt (summary) | Commit | Result |
|---|---|---|---|
| 1 | Set up repo, commit spec + rules + build log scaffold | `ddd8dea` | chore: initial spec and build log scaffold |
| 2 | Build pipeline (src -> single index.html), CONFIG block, seeded PRNG, wind field, map generator with the 11 validation invariants + headless node tests | `949c777` | feat: build pipeline, CONFIG, wind field, deterministic map generator |
| 3 | Game state, route pieces + sockets + placement legality, support BFS with island conduction, progressive collapse + reconnection, headless tests | `c670ec3` | feat: game state, route network, support graph, progressive collapse |
| 4 | Renderer (islands, temples, sea, wind tells), tap placement UI with wind-multiplier ghost, portrait camera framing, first screenshots | `2760056` | feat: renderer, portrait scene, tap placement UI, main loop |
| 5 | All remaining systems: economy/hauling/adrift/priest, structures/claim, combat, 9 waves + AI powers, Poseidon AI, fog, player powers, WebAudio, tutorial/banners/win-lose. Player-directed: scrollable multi-screen map (CONFIG-sized), exploration shroud, "TAKE THE WIND" CTA, GitHub Pages hosting | `5880a6e` | feat: full game systems |
| 6 | Opaque shroud boxes, lighting rebalance, LICENSE, Pages, repo metadata | `c64ae95` | fix: shroud boxes prevent edge peek; lighting rebalance |
| 7 | Playtest-feedback round (live play on Pages): island-coast sockets, ghost+RAISE flow for structures, ROTATE button, socket beacons + hints, toasts to top, intro lore carousel, combat tracers, wind-ripple sea, allegiance-tinted island bases, pinch zoom + two-finger rotate, opaque fog, whitecap respawn fix. Systems: AI objective blacklist fix (livelock), scaled influence radii, win-reachability invariant 12, real-input Playwright test, win-path test | `4fc2d22` | feat: playtest UX round |
| 8 | Great Temple HP bars + edge threat warning; influence lifts the shroud (player couldn't build into fog they were entitled to build in); Wave 5 signature sequence verified end-to-end by test; fillers get standard plots | `cc22221` | feat: temple bars, influence-lit shroud, wave-5 gate test |
| 9a | Drift lines, fog-scaling zoom, README refresh, corridor wind motes | `10e3365`..`e773868` | small feature commits |
| 10 | **Two more player-directed rules:** (a) Poseidon's craft operate only in waters his lane network reaches — he must build toward you to strike you, and cutting his lanes literally pushes his reach back; (b) roads ARE reach — route pieces and endpoint towers are no longer influence-gated, so you can drive corridors into his waters to lift fog and get guns onto his infrastructure. Island emplacements, temples-for-income and fog-lift stay influence-bound. Fallout fixed: both economies stopped saving for temples once the influence gate stopped acting as an accidental piggy-bank (AI temple fund + script fix); wave-5 test gives Poseidon his realistic lanes. Legibility: quarry pits + headframes on ore islands, hit-shudder no longer levitates structures (Y accumulated per frame), Phong water with bump-mapped ripples and swell | `a168820` | feat: lane-reach warfare, roads-are-reach, watery water |
| 11 | Small rounds pushed live: fog-scaling zoom `10e3365`, README `34f3468`, drift lines `e773868`, lane-reach + roads-are-reach + water `a168820`, opening economy + tap-identify + currency colors `02a15e1`, design intent `1e74659`, README 8 `d5d4048` | (hashes inline) | several |
| 13 | Compact top banners; Poseidon theatre to intro `e6d86f2`; spec coverage matrix `6c10075`; gauntlet test `bf81791` | (inline) | several |
| 14 | **Player-directed batch:** 3×3-screen world (18×30 — chain bridging rewritten as greedy arm-growth after 98% failures on diagonal gaps); persistent wind-streak layer scaled to water area; realistic water (depth-tinted shallows, coastline foam, Phong bump ripples); guns may cut any unsheltered lane while craft stay raw-end-only (felt asymmetry fixed); stranded balloons slip their moorings and drift after a grace period; Cross piece (4-way, 7 supply); weapon placement shows every legal site + range ring from the ghost, taps snap within 1.35 cells; power/tech buttons explain themselves | `182c630` | feat: wide world, watery water, armed symmetry |
| 15 | Wide-world batch shipped | `182c630` | feat: 3x3 world, persistent wind map, realistic water |
| 17 | Thumb-side pieces + floating confirm `3440fdb`; prong-classed hand, thumb reroll, gem mines, AI attack lanes + cooldown fix, 100/100 start `10fe49d` | (inline) | several |
| 19 | **Mining by connection (player-directed):** any island your supported network touches that is neutral or yours mines automatically and haulers collect from it; a rival's claim shuts the quarry to you — claiming SECURES ore rather than enabling it. Context menu gains a compact balance line (⚇ · ✦) at its top | `1fdd22a` | feat: connection mines, claims secure |
| 20 | Mining-by-connection `1fdd22a`; topsafe HUD fix `0b458ef`; reserves 500–1500 + delivery ching `07dd0e3`; hauler cargo ×4 `9f6cdfb`; favor-priced roads `eb6838a` | (inline) | several |
| 23 | **Hauler capacity sweep** (`test/opt_capacity.js`, hot-air value with hydrogen locked at 2×): {6,12,24,48,96} → scores {89,**103**,97,101,86}. 12 produced the best single match of any sweep (nine waves survived at 108 HP, 46 kills, 5 reconnections, balanced reserves). 24+ floods supply (600–1500 unspent) now that reserves are 500–1500 and any connected island mines. **Set 12/24 — deliberately walking back half of the earlier ×4 directive on the data**; player may override | `e69db1a` | tune: hauler capacity 12/24, sweep-derived |
| 24 | **Fair-play audit + lane legibility (player report: "Poseidon draws random paths near my islands unconnected to his network"):** new permanent `test/test_fairplay.js` independently re-walks his segment graph from his Great Temple every 2 sim-s across 3 full matches — zero orphans ever kept alive; every cut lane dies on its collapse timer. The report was a *readability* failure: his trunk lanes ran through unexplored shroud, so tips materialised parentless at the fog edge. Fixes: enemy lanes now ghost one segment beyond live vision (paths visibly continue somewhere), tapping any lane names it and its state (his: "TRACES HOME TO HIS TEMPLES", cut: "FRAYING/UNBINDING"), and a once-ever line states the law when his lane first nears your holdings | `a41f013` | fix: lane fog legibility + fairplay test |
| 25 | **Match film + fresh README slate (player-directed):** Playwright rig records a ~7x time-warped nine-wave match on the reference seed with a camera director that pans between the frontier and live fights (`docs/media/match_timelapse.mp4`, teaser GIF inline); all nine README screenshots retaken against the current build (old slate predated 3x3 world, thumb menus, quarries, fog boxes); README prose de-staled (2x3→3x3, influence-gated placement→location-first roads-are-reach, 11→12 invariants) and restructured into 8 feature sections incl. the fair-play audit and sweep-tuning story | `c25fd52` | docs: match video + new screenshot slate |
| 26 | **The optb turtling anomaly was an AI livelock, not a map quirk:** every player gun-cut set his 4s "reaction pause" anew, so under sustained bombardment (cuts faster than 4s apart — common where the networks interleave) the pause rolled forward forever and froze ALL his building; waves kept spawning lane-bound craft onto a dead network (40 idle craft by minute 7). Diagnosed with `test/diag_optb.js` timelines (segsP 62→19, flat forever). Fix: the pause is rate-limited to once per reroute-cooldown window; area-avoidance (avoidCells) still applies per cut. He now rebuilds through bombardment | `6acbbfd` | fix: rate-limit AI reaction pause |
| 27 | **Four-front sweep batch** (shared harness `test/opt_lib.js`; 75 scripted matches): **difficulty grid** (`opt_difficulty.js`, ramp x gun-power, 36 trials) — current 0.15/1.4 + GUN_VS_SEGMENT 0.45 wins at 103, and 0.45 beats 0.30/0.65 in every difficulty row: both knobs now data-validated. **Pacing** (`opt_pacing.js`) — x1.0 (103) crushes x0.8 (83, starves his buildup) and x1.25 (62, waves toothless: 2 kills). **Collapse tempo** (`opt_collapse.js`) — macro-insensitive from 1.5 to 900s/segment, so it is a pure feel knob; set 3.0→2.0 so severed enemy branches clear faster (board honesty, follows row 24). **Costs** (`opt_costs.js`) — defense costs show no signal (keep 7/10/12); hauler cost monotone and better on every seed: 8→**5** (110 vs 103 vs 77 at 12). All post-livelock-fix: optb now plays real matches | `153b28c` | tune: sweep batch - hauler 5, collapse 2.0 |
| 28 | **NetStorm batch (player-directed): salvage + kill bounty + solid defenses.** Salvage: own structures half cost back (⚇), segments unbind tip-first for 1 ✦; no explosion but support recalc runs. Bounty: downed craft pay 2/3/5 ✦ by class (adrift losses count — a cut IS a kill), combat-killed structures pay ¼ cost ⚇. Defenses zero-port and solid: no attachment, no crossing; on a route they block transit for both sides (ship mid-journey goes adrift; rebound never moors to a defense); on island ground they do NOT seal the island (first cut sealed Great Temples — home plots caged the GT and every hauler starved, caught by test_sim deliveries=0). Scripts learn plot-away-from-anchor siegecraft. Buildlog audit per INSTRUCTIONS: 14 `*(this commit)*` placeholders replaced with real hashes recovered via `git log -S`; log now lags code commits by one step as specified | `0320e38` | feat: salvage, kill bounties, defenses seal the road |
| 29 | **Airfield yards + hot-air balloons (player-directed):** yard rebuilt as hangar + mooring mast with pennant and live fleet-status dots (one per mooring, lit while held); tapping a yard opens a yard-only dialog (BUILD HAULER with moored count, whole-fleet UPGRADE HYDROGEN, SALVAGE); hot-air haulers are free balloons — round envelope, basket on cables, burner flaring on each ship's own rhythm; hydrogen refit rebuilds the fleet as zeppelins mid-match. Subtitle → THE WINDS INHERIT THE WAVES (sea-to-air transition) | `f18a176` | feat: airfield yards, burning balloons, subtitle |
| 30 | **Endgame siege fixed (player report: only wall-spam + bolt could kill his temple; his guns shred half-built batteries):** Wind Wall was never stacking (one wall, recast moves it) — made it THE siege tool: radius 1.2→2.0, duration 12→15; directional guns now silence armed structures before battering temples (symmetric); once-ever hint to wall a site inside known enemy gun range. New `test/test_siege.js` proves the pattern vs his DEFENDED temple (test_win never spawned the authored defenders): walled bolt survives raising, defenders fall armed-first, temple falls, 6 recasts = 24 ✦ upkeep | `609951f` | fix: siegecraft, not wall spam |
| 31 | **Cinematics (player-directed: "more cinematic so long as it doesn't interfere"):** wind-riding stylized clouds with sea shadows (opaque puffs — transparency depth-fought the sea and read as hollow bowls); surf crests bursting white against all four arena rims (M=2.2 outside; three look iterations — dark crests read as holes against lit sea); one slow camera breath outward during the 8s telegraph, applied as a multiplier so pinch always wins (updateCamera only ran on input — the breath drives its own refresh); coin bursts on bounties and deliveries; Wind Wall dome drawn at its true buffed radius. All original geometry — the suggested ocean/cloud/tree libraries (poseidon, gaia, volumetric clouds) stay out per contest originality rules; techniques only | `8ca727a` | feat: cinematics batch |
| 32 | **His voice is gone from matches (player: "temple toast and poseidon's complaint — gone — ever!"):** real culprit found — the codex long-press fired "Poseidon's Complaint" whenever a finger rested ~1s on water (constant while pinching zoomed out) and the Temple entry on island holds; codex trigger deleted. Also removed: tide banners, ISLAND CLAIMED, wave-1 lore, his tutorial quote; Wrath/priest-lost/restored shrink to tickers. Same commit: Aegis = 360° bound whirlwind (art: spinning vortex bands + wisps), pricing doctrine (physical→⚇, magic→✦, both→both: temple 14+4✦, aegis 12+4✦, dual costs in menus/AI), bolt preview draws its 90° turret wedge, clouds removed, three-finger tilt | `88bac48` | fix: voice gone, whirlwind aegis, pricing doctrine |
| 33 | **Real water (player-directed; rules re-checked):** contest rules permit rights-cleared content ("original or use assets the Entrant has rights to"; libraries belong in /vendor) — the earlier all-original constraint was our own session rule, now relaxed by the player. Sea replaced with three.js's own Water shader (`vendor/Water.js`, MIT, part of the already-vendored library): planar reflections, sun glare, per-pixel waves, shader time paced by wind. The normal map is still GENERATED at runtime (canvas) — zero image assets added. Old Phong sea kept as automatic fallback. Suggested external ocean/cloud/tree repos still declined — unvetted licenses, and the vendored-library route covers the need | `da6ad9e` | feat: real water via three.js Water shader |
| 34 | **Storm surge instead of blob breakers (player: "look bad, just blobs — be the water or don't"):** rim breakers deleted; on each wave telegraph the WHOLE sea rises (Water-shader distortion + wave speed swell through the 8s warning, rage ~20s, ease back). Canonical `waternormals.jpg` mirrored into /vendor per player instruction (runtime canvas normals stay as the file:// fallback — browsers can refuse local images as WebGL textures, which is the classic failure). Bolts aimable at placement (wedge ghost + TURN traverses 45°, facing commits — player asked after seeing the new wedge). Fog boundary unified: enemy lanes/structures draw wherever ground shroud is lifted (explored ∪ influence), killing the clear-water ring that hid lanes until the live-vision bubble arrived; craft still need live eyes | `b449911` | feat: storm surge, aimable bolts, honest fog |
| 35 | **Gesture trap escaped (player: stuck in pan/tilt/zoom, taps dead):** a missed pointerup left a ghost entry in the pointer map, so every later touch read as multi-finger forever. Four defenses: primary-touch purge (a PRIMARY pointerdown proves no other finger is truly down), setPointerCapture (guarantees the matching up/cancel), lostpointercapture cleanup, and blur/tab-hide forgetting all gesture state. `test/test_input.js` (real Playwright input, replaces the stale scratchpad rig) injects orphaned pointers and proves a tap still opens the menu. Player's console dump also caught a live crash: the waternormals upgrade set `uniforms.waterNormals` but the Water shader's uniform is `normalSampler` — the canonical texture never applied on Pages and threw on image load; fixed + guarded, favicon 404 silenced | `362431c` | fix: gesture trap + normals uniform crash |
| 36 | **Wind-true water + desktop mouse + bombardment audio (player-directed):** (a) the Water shader's four hardcoded noise-scroll vectors patched to a wind-driven `flowDir` uniform (world-space; smoothed 0.8/s so camera pans across sheared wind never snap the ocean) — the sea and the whitecaps finally move together, and speed still scales with wind + storm surge; (b) full mouse controls: wheel zoom, right-drag orbit+tilt, context menu suppressed, right-clicks never tap; (c) player: "attacking our temple sounds like a slot machine" — greatTempleHit streams at damage-tick rate and played two square bleeps per event; now one heavy masonry boom max/1.2s + stone-chip noise, rhythmic debris embers paced with it, and a separately-throttled dull impact crack for shots landing anywhere | `5612e23` | feat: wind-true water, mouse, bombardment audio |
| 37 | **Fleet remodel + five playtest reports (player-directed):** all ship models rebuilt — hot-air = gored free balloons (meridians, crown, skirt, wicker basket), hydrogen = Santos-Dumont dirigibles (cigar envelope, girdle bands, cruciform fins, keel gondola, spinning pusher prop), priest = gilded balloon carrying a four-column flying shrine, Poseidon = true triremes (waterline ram, curled sternpost, oar banks; shields/armor/pump/amphorae by class); materials cloned per ship so venting/burner stay individual. Aegis shows its protection zone (wisps swirling at full cover radius). Pan no longer races the water (churn calms to 12% while dragging — reflections already sweep with the eye). Depleted mines explode and become bronze BUILD PADS (new plot at the quarry cell). Islands trail lee wakes downwind (fog-gated) | `a9f68a0` | feat: fleet remodel, aegis zone, calm-pan water, mine pads, wakes |
| 38 | **Flat-blue water on real GPUs (player: "not watery at all, no waves"):** the wind-flow shader patch declared a variable named `cross` — a GLSL BUILT-IN. SwiftShader (where all verification screenshots render) tolerated the shadowing; stricter real GPU drivers refused to compile the fragment shader, so the live site showed flat normal-less water while every local capture looked fine. Renamed `flowSide`. Lesson recorded: never trust SwiftShader alone for shader changes — it is more permissive than production drivers | `e3bc1de` | fix: GLSL cross shadowing broke water on real GPUs |
| 39 | **The sea was frozen at time zero (player: still no waves):** live-debugged IN the player's browser via MCP — shader compiled clean but `uniforms.time` never advanced. The pan-calm smoothing seeded itself inside one expression, so frame one computed `target + (target − undefined)` = NaN and the water clock was NaN forever; SwiftShader's reflection pass still mottled local captures, hiding it. Hot-patched the live page (waves returned instantly), reproduced the flat-blue frame on clean reload, then shipped the two-line seed-before-smooth fix and re-verified on the deployed build | `16879ac` | fix: NaN froze the sea |
| 40 | **Pan-boil + invisible flow (player report):** more live-debugging showed flowDir WAS tracking wind and wakes existed at 0.19 opacity — invisible; the "crazy" pan was reflection DISTORTION boiling under a moving camera (previous calm only slowed the wave clock, one-finger pans only). Now every gesture registers camera motion; while moving, distortion damps to 18% and churn to 12%, easing back at rest. Islands visibly part the current: brighter longer lee streaks + new bow-foam arc on the upwind coast (fog-gated). Primary downwind scroll time/17→time/11 for readable travel. Verified deployed: real drag panned cleanly, 6 bow arcs live | `6e47af1` | fix: calm all camera motion, visible island flow |
| 41 | **World-anchored water (player: the ( and = stripes look bad; pattern slides off islands while scrolling):** bow arcs + lee streaks deleted — painted geometry keeps reading as glyphs, so the coast foam, whitecaps and wave field carry the flow language alone. Root cause of the sliding: the mirror layer moves with the CAMERA, and at full fresnel it dominates — reflectance capped at 45% so the world-anchored pattern is what you see; motion calm strengthened (churn 5%, distortion 10%, 300ms window, all gestures) | `396759f` | fix: world-anchored water |
| 42 | **Clean bottom bar + ship avoidance (player-directed):** bar is one row (TAILWIND / WIND WALL / HYDROGEN); mini-hand chips and prongs note removed — the thumb menu already owns pieces, reroll and discard. Ships hold a side of the road and lean wider when another is within 0.6 cells (render-only dodge; sim untouched). Input test still green without the hand strip | `36e4899` | feat: clean bar, pass-around ships |
| 43 | **Waves are EVENTS + cloudy fog + flow math + fog leaks (five reports):** wave launch now lands with a war-horn crash, an eruption at the origin, a pulsing teal storm vignette (telegraph→assault) and an edge chevron pointing at his fleet when off-screen — drama without a single word. Ocean lurch on pan root-caused: scroll was flowDir×absolute-time, so wind changes were amplified by all accumulated seconds; now a CPU-integrated offset (direction changes affect only the future). Floating mines on seed w8rlfq: the quarry kit was never fog-gated — all of it now obeys groundLifted. Fog of war = dense billowing cloud (original fbm shader, opaque). Parked units each hover over their own island square | `e441f74` | feat: wave events, cloudy fog, flow integration |
| 44 | **Quarries occupy their ground (player caught his temple ON a mine):** no building on the mine cell for either side until depletion blasts it into a pad — gate in whyNotBuild + quarry-aware plot picks in AI, context menu, and all scripted players (test_win had picked a filler''s quarry plot and failed until guarded). SKIP → SKIP TUTORIAL. `node serve.js`: stdlib dev server on :8080, auto-rebuilds on src/shell changes | `11b0d31` | fix: quarry ground, labeled skip, dev server |
| 45 | **Tap-identity + fortify-anywhere + wind-pushed fleet (player-directed):** context menu now opens with an identity header (ship under thumb: class/hull/cargo/state, then the ground) plus available actions — nothing stays a mystery. Every empty island tile is buildable (plot founded at the tapped cell; quarry ground still reserved). Dirigible propeller removed on LORE (nothing self-propels; bow wind-scoop + streaming pennants instead). `opt_headwind.js` sweep: one-way lanes rejected (drifting wind strands fleets), but spread widened AIR 0.55–1.45 so circuits beat out-and-back. Ore heap leans toward island centre (was hanging over water on coastal quarries = "mines mid-ocean", w9iwff); bridge placement that seed verified live in-browser (menu→chips→ghost→confirm) | `2f0780c` | feat: identity menu, fortify anywhere, wind lore |
| 46 | **THE AGE OF AIR (player-directed):** subtitle sharpened from THE WINDS INHERIT THE WAVES; the 7-card rotating lore carousel is retired for ONE four-sentence paragraph above TAKE THE WIND (Odysseus'' lost winds → guild binds them into roads → the sea unpaid → ''Poseidon is not insulted — he is passed over. And he is rising.''). README + intro screenshot updated | `fc175a3` | feat: age of air intro |
| 47 | **Lore line tightened (player-directed):** ''not insulted'' cut — ''Poseidon is passed over — and the tide is coming to collect'' (revenge tied to the unpaid crossing toll) | `c670ec9` | tune: tide comes to collect |
| 48 | **Play either god (player-directed):** BIND THE AIR / RIDE THE WAVES. The sea side is a full PRESENTATION mirror via one `airSide(side)` helper threaded through every side-keyed visual (segments+altitude, mover meshes, chassis, temples, tints, names, end screens): your lanes lie dark on the water, your fleet is triremes, the enemy becomes the Air Guild. Sim untouched — side A is still the network-builder — so all tests/sweeps/fairness proofs cover both skins. Intro names both factions and ends ''Choose your side''. Real 5-line tutorial (pathways → mining/haulers → coin/temples → Favor/divine aid → cap ends) replaces the cryptic quotes; PowerShell quote-escaping briefly shipped a syntax error (caught by the input test — the page failed to parse) | `9447e5c` | feat: two-sided CTA, real tutorial |
| 49 | **Poseidon side completed (player: still had air weapons + air powers):** makeChassis branched on the literal side — guns hung from balloons regardless of theme; chassis now themed (your arms are moored barges, the Guild''s are balloons). Bottom bar themed: FOLLOWING SEA / BREAKWATER / DEEP HULLS (same numbers). Structure labels/names themed: STORM DRUM, LANCE BATTERY, BULWARK WARD, SHIPYARD; refit ticker + yard dialog follow; tutorial power line god-neutral. Verified: sea-theme match to defeat shows themed bar + themed end line over teal lanes vs Guild sky-roads | `9563e9f` | fix: complete the sea loadout |
| 50 | **Sea-side previews grounded (player report):** ghost/beacons/action markers hardcoded AIR_ALTITUDE — as Poseidon the provisional route floated in the sky before landing on the water. New `playerRouteY()` puts all provisional visuals at the player''s network level; tracers and unravel fx follow themed altitude. Verified: sea-theme ghost hugs the waterline | `4990248` | fix: waterline previews |
| 51 | **Priest ashore + shrine-ship + AI garrisons + sea art + fixed sectors (player-directed x5):** priest on an island renders as a robed FIGURE with staff (vessel only under way); sea priest sails a royal-blue processional shrine-ship (marble columns, glowing teal dome, trident bow). AI now garrisons: drum on every claimed island + forward lance every ~45s ("he never places guns"). BULWARK WARD = circling stone sea wall + foam crests (not an air whirlwind); SHIPYARD = stone slipway/piers/crane. Weapon rework: omni radius 2→3, directional guns FIXED-SECTOR (no traverse; sector-filtered targeting; permanent faint wedge everyone can read). Full battery green incl. siege; difficulty grid re-sweep queued | `2a71065` | feat: figures, garrisons, sea art, sectors |
| 52 | **Wave ramp re-swept post-rework:** garrisons + fixed sectors raised de-facto difficulty (old 0.15/1.4 fell 103→88 in the grid; optc dying wave 4 in several cells); softened to 0.10/1.2, gun mult stays 0.45 (0.65''s 101 vs 99 is noise). The lesson holds: every mechanics change re-prices the tuning | `1721706` | tune: soften ramp post-garrison |
| 53 | **Fixed sectors re-priced the lane war (sim went red — committed before reading the battery, caught one commit later):** no traverse downtime = ~100% gun uptime, so GUN_VS_SEGMENT 0.45 ground both networks to dust (496 cuts/match, craft idle, zero engagements). Spot sweep under the new kit: 0.12–0.3 healthy → set 0.2. Garrison capped (4 drums + 3 lances — uncapped he built 22 structures and blanketed the map). Sector wedges shrunk to a discrete hull marking (player: ''they paint the whole map''). Lesson repeated in caps: every mechanics change re-prices every tuned number | `0e0ae71` | fix: lane-cut re-price, capped garrison, hull wedges |
| 54 | **Winner study applied (a/b/c) + powers into the menu (player-directed):** researched the design-phase winners (Chonky Corgis, Hearthline): they win on ONE resonant mechanic, minute-mapped first sessions, charm, honest scope. design-intent.docx rewritten (492w, same 7 sections): ''one road, five jobs'' pillar, nine-tides constraint, first-session beat map, sweep+fairness as method; README mirrors it. Tailwind/Wind Wall moved into the thumb menu (wall casts on the tapped spot — better than arm-then-aim), hydrogen already in the yard dialog, bottom bar now EMPTY. Charm pass: tapping open water names the ruling Anemoi (Boreas/Notus/Eurus/Zephyrus) + strength | `7a75cfc` | feat: docs, menu powers, named winds |
| 55 | **Power-discovery shimmer (planned follow-up from row 54):** first menu that shows the relocated TAILWIND / WIND WALL HERE buttons glints them 3× (gold pulse); marked seen once-ever at actual display | `6056702` | feat: one-time power shimmer |
| 56 | **Wind Wall de-domed (player: it hides everything it covers):** the sheltering dome occluded its own contents; now a bright ground ring at the true radius + swirling rim posts, breathing, fading over its final 2s — contents fully visible | `8112de7` | fix: wall is a boundary |
| 57 | **Guided hand-holding tutorial (player: people don''t understand what to do):** timed lines → a step MACHINE with a pulsing ring + pointing finger at the exact touch target; steps gate on real actions (path laid → delivery lands → consecration starts → yard built → gun built); the wave schedule slides while it runs (sims unaffected); once ever, SKIP exits. Tutorial text now sits in a dark bordered pill (was unreadable over water). Verified: step 1 renders with hand on the coast, input test passes with tutorial active | `80fc152` | feat: guided tutorial, readable pills |
| 58 | **Confirm row restyled + twin turns + chip relocation (player-directed):** CONFIRM/CANCEL/turns now share the context menu''s exact visual language (dark bordered panel, shown only while a ghost is up); TURN split into ↺/↻ working for both piece orientation and bolt aiming; PRIEST chip to lower-right; ticker dropped below the tutorial pill to stop collisions | `ba79690` | feat: unified confirm panel |
| 59 | **Categorized menu + WATCH A MATCH (player-directed):** menu buttons grouped under compact label rows (ATTACK/SHIELD/WORKS/FAVOR/DESTROY/PATHWAYS, pathways nearest the thumb); third title CTA runs a computer-vs-computer demonstration (sweep-script player + fight-chasing camera, spectator pan allowed, tap exits). Verified live: labeled groups render, demo self-plays with pill | `2efb4b2` | feat: menu categories, demo mode |
| 60 | **Hordes (player: waves don''t feel like hordes; countdown unclear):** attack lanes now start at the FIRST TELEGRAPH (was: only after craft existed — early waves idled far off and the countdown paid off with nothing); compositions re-cut to numbers (transports 3→8 across the arc, HP 42→32 each). Battery green; grid re-sweep queued | `0c4a67a` | tune: horde waves, telegraph-driven lanes |
| 22 | **Favor income sweep** (`test/opt_income.js`, `opt_income2.js`): divisor-only sweep showed lean income causes death-spirals (lose temples → can't afford roads → segsA collapses to 10); pairing a leaner territory rate with a fatter territory-free baseline fixes it. Pairs [8,4] and [16,6] tie at 92; chose **divisor 16 + baseline 6/10s** because actives end at 24–31 Favor (every power cast competes with roads — the tension we wanted) vs 100+ spare at [8,4]. The baseline drip is rebuild insurance | `dfc95e5` | tune: favor income 16-divisor + 6 baseline, sweep-derived |
| 21 | **Favor price sweep** (`test/opt_favor.js`): prices 1/2/3/5 score identically (avg 90 across 3 seeds) because temple-influence Favor income floods the price; at 5 active players end at 0 Favor (powers become unaffordable — the sim can't see this since the script never casts); at 8 networks starve on 2 of 3 seeds. **Chose 3**: quality identical to 1, reserves halved, price felt, powers budget preserved. Flagged follow-up: Favor *income* (hundreds per match from temple influence) is the real oversupply lever | `3baed33` | tune: piece price 3 favor, sweep-derived |
| 18 | **Toast discipline (player-directed):** every transient line (tutorial, banner, ticker, codex) is tap-to-dismiss; story/tutorial lines show once EVER via localStorage (skip marks them all seen); codex long-press slowed to ~1s so it stops firing accidentally | `fe63d05` | fix: dismissible, once-only story toasts |
| 16 | **Player-directed: location-first building.** The piece palette is gone as an interaction: gold markers glow on every actionable spot; tapping one opens a context menu at the thumb with the held pieces that fit there plus every valid building (all explained). The hand is a read-out strip; tap-twice discards. Real-input test rewritten for the new flow | `2c8dc10` | feat: map-first context building |
| 12 | **Player-directed: Poseidon's units are lane-bound.** His craft ride his lane network exactly like his haulers, halt on the lane within weapon range (stationary attackers while engaged), and go adrift if the lane is cut beneath them. Craft HP raised (42/62/112) because single-file approaches are easier to shell; transports get a 2.2 assault reach inland. With this, the naive scripted player now survives all nine waves — pressure comes from his expansion, exactly as directed. Also: unique art per structure type (rust vane drum with spinning arms, navy zeppelin battery, bronze shield pylon), burning states on wounded structures, muzzle flares, flying embers, attack sounds (throttled), self-explanatory build menus with one-line descriptions | `5244bf0` | feat: lane-bound assaults, unique unit art, combat noise |
| 9 | **Rules change (player-directed): paths are invulnerable along their length.** Only raw open ends (uncapped tips) and structures can be attacked; a tower caps its tip; islands moor their ends. Symmetric for Poseidon's lanes. Networks now erode from open tips, and the collapse drama comes from *inline* structures exploding (routes continued through a tower's ports) — which is exactly the §14.0 shape. Wave-5 test updated to the inline-battery shape and passes. Tidal Surge keeps its mid-corridor bite as the one divine exception | `d80a0e7` | feat: raw-end exposure rule |

**Wave 5 gate (spec Day-10 gate) — PASSED by automated test:** scripted heavies target the most forward player structure, its death explodes the outward segment, the branch frays, a rescue link relights everything. The test also caught that an undefended Great Temple dies to wave-5 transports before the drama even lands — which is the game working, but worth knowing for tuning.

**Session 1 addendum — design changes directed mid-build:**
- Map no longer fits one screen. World is sized in portrait-screen tiles (`MAP_SIZE`, default 2×3 → 12×30 cells) and the camera pans by dragging. Island count scales with area: stepping-stone "filler" islands grow automatically wherever the island-hop chain stretches beyond hop range, plus scattered neutrals per extra area. Spec distance invariants scale from the 12×20 reference.
- Exploration shroud added (deviation from spec §14B "terrain is never fogged", by explicit direction): unexplored terrain is dark until the player's reach lifts it, one-way. Enemy-entity fog stays as specced on top of it.
- Favor income tightened: only island Temples mint Favor from influence; the Great Temple's own radius doesn't (raw §14.5.4 made Favor meaningless at ~50/10 s).
- Repo made public with an all-rights-reserved license; live build hosted on GitHub Pages per direction. Contest zip releases only on instruction.

**Session 1 addendum 2 — first live playtest feedback (the player actually played the Pages build):**
- *"Confusing to place a sky channel"* → whole coastlines of conducting islands are now sockets (a channel terminal may start anywhere an island touches — also a rules change the player asked for directly), sockets got beacons and pulse, single-socket cases auto-preview, step-by-step hint toasts, and an explicit TURN button beside CONFIRM.
- *"Built a mooring yard, nothing seemed to happen"* → structures now use the same ghost-then-confirm rhythm as pieces (menu arms a translucent ghost; RAISE builds), scaffold ring moved to ground level, Great Temple mesh shrunk so plot structures stop hiding behind it.
- *"No attacking animations"* → tracer fire: gold falling ballast for Aeolus, teal rising jets for Poseidon, impact sparks, throttled per gun.
- *"Fog should be opaque"* → shroud boxes fully opaque. *"Wind animations pool at the edges"* → whitecaps live a few seconds and respawn at random water instead of wrapping (the sheared wind ran edge-parallel and herded them).
- *"Ripple the water with the wind"* → generated tileable swell texture on the sea, streamed along the live wind vector. *"Backstory on the intro"* → five-line lore carousel above TAKE THE WIND.
- Balance found by simulation: Poseidon AI had a planning livelock (fixated on unreachable islands scored by influence overlap — blacklist + hard influence gate fixed it); after the fix he snowballed, so siphon DPS 8→6, transport DPS 5→4, wave strength per temple 0.2→0.15. Influence radii now scale with map size (leapfrog broke on larger maps) and validator invariant 12 guarantees the territorial win path exists on every map.
- New standing rule: the build log is updated with every change and committed with every commit (the row for a commit still lands one commit later, since a commit cannot cite its own hash).

**What I built:** Development skeleton the spec asks for (§33F.1): game logic in `src/*.js`, `build.js` concatenates into one readable unminified `index.html` with the frozen `CONFIG` block first. Three.js r147 (UMD build, works from `file://`) vendored under `/vendor`. Wind field (6×10 vector field, bilinear, ±30° drift). Deterministic map generator: zone-constrained island growth, mirror-then-perturb for the economic corners, all 11 validator invariants from §20A.5, re-roll with verified golden-seed fallback. Headless node test harness runs the pure game logic without a browser: determinism, convergence (200/200 random seeds resolve without fallback), golden seed verified.

**Key decisions and why:**
- Three.js r147 UMD instead of newer ES-module builds: judges may open `index.html` from `file://`, where module imports fail CORS. A script tag always works.
- Wind is a cross-map **shear front** (bearing rotates with perpendicular offset from the player's outbound axis), not uniform wind or a gyre. A gyre made winds perpendicular to radial corridors (useless); a shear front guarantees the §21A.6 requirement that outbound and return corridors ride different bearings.
- Invariant 3 ("every efficient route crosses ≥5 over-water cells") is checked on the straight monotone route (always a shortest path, since nothing obstructs air routes), with invariant 4's 1.4× ratio pricing safety. A literal "every shortest path" reading was uncheckable cheaply and over-strict on a 12×20 grid.
- The "exposed" interior island is usually the **chokepoint**, not a Sacred island: on a Manhattan grid, an island reachable by a monotone island-hop chain can never make the safe route 1.4× longer, so the exposed role migrates to the centre island where the hop chain must detour. Deviation from §20.2 noted deliberately.

**Pivots, and what changed my thinking:** Superseded the "one handcrafted map" locked decision — the spec itself (§20A) supersedes it. First generator attempt (free zone packing) converged 0% — island separation (min distance 3), hop gaps (exactly 2 water cells), influence radius (supply fully inside 8) and home-to-interior distance (8–12) interact so tightly on 12×20 that random placement almost never satisfies all of them.

**Biggest problems, and how I solved them:** Generator convergence. Three fixes: (1) grow islands under a "forbidden" halo so separation holds by construction, only mirror the two corner pairs; (2) pull chain islands toward their neighbours — the separation halo stops growth at exactly a two-cell water gap, which is one island hop; (3) a deterministic bridging pass that extends a chain island by one lobe cell when the hop is misaligned (a 2-water hop needs straight alignment; Chebyshev-close but diagonal pairs have no hop). Went from 0% to ~18% per attempt = effectively 100% of seeds within the 50-nonce re-roll budget.

**What I learned:** Constraint-packing on a small grid needs constructive guarantees, not rejection sampling. Also: measure per-invariant failure counts before tuning anything — the first three "fixes" I guessed at were aimed at invariants that weren't the bottleneck.

**Where things stand / next:** Renderer + portrait scene (islands, sea, wind tells) so the Day-3 legibility gate can be tested; then route pieces + support/collapse.
| 61 | **Demo plays the real UI (player: 'could the ai be using the menus too with buttons that are selected being hilighted?'):** WATCH A MATCH is now a puppeteer — opens the true context menu at its target, gold-pulses the button (data-key finder), clicks the real handler, walks ghost→TURN→CONFIRM; camera holds on the act. Two stalls found by live probe: priest plan fired before any road reached (starved path-laying — now route-gated) and SEND PRIEST targeted a plot cell whose menu lacks the button (now island-body cell). Same batch: wind-bound hauler routing (Dijkstra, legs under 0.8 headwind closed; loaded balloons WAIT for the shift; hydrogen exempt — the refit earns a second perk; cargo loads only on departure so waits don't double-take ore), menu category COLUMNS (ATTACK/SHIELD/FACTORY/RITES/FAVOR/DESTROY), wave chip narrates ('TWO SHIPS EN ROUTE'→'ATTACKING'), end screen AGAIN + BACK TO MENU, intro de-duped ('harnessed…roads of bound air'), gun-vs-road 0.2→0.3 per horde-era 36-trial grid (top cell avg 97 at ramp 0.10/1.2). Battery green; economy alive under the wind law (47 deliveries) | `db889ec` | feat: demo puppeteer, wind-bound routing |
| 62 | **Wind law of the roads (player, three rulings: 'cant go against the wind' -> 'equally apply to poseidon' -> 'loop the airflow?' / 'air god on our side'):** haulers Dijkstra-route on RAW prevailing wind (favorable channels win; a built loop circulates - out with the wind, home on the other arc); never opposite a channel; own bound channels floored at 0.4 of the band (air 0.91, sea 0.98) so roads always work; hydrogen/Deep Hulls exempt; tack-after-20s valve. Strict closure alone was proven fatal by headless trace (reference road legs 0.704/0.585 never open under 30-deg drift - zero deliveries a whole match); the bound floor restored 63. Also: demo puppeteer cell picks now obey whyNotBuild (bare occupancy test missed the GT footprint - user watched it loop OCCUPIED on the home vane) | `5e1f225` | feat: bound-channel wind law |
| 63 | **Visible channel flow (player: 'can the wind flow in the channels be visible'):** the old tell was 4 near-white 0.035-radius spheres on air ribbons only - invisible against the ivory, absent from sea lanes. Now: elongated streaks on BOTH sides' channels in the wind's true direction, gold-on-ivory / foam-cyan-on-dark (first capture proved white-on-white unreadable), length+speed scale with alignment (held-against legs show short slow stubs), end-fade kills wrap pop, fog-dim remembered lanes show none (live flow would leak the wind). Grid re-swept under the wind-law economy: gun-vs-road 0.3 -> 0.45 (avg 107 vs 96, same ramp 0.10/1.2) - reliable roads = richer economy = lanes can bleed faster. Battery green | `f1cb30d` | feat: visible channel flow |
| 64 | **AI never corks its own lanes (player: 'doesnt block its path with attack items... chooses a 3+ bridge to put an attack item on a non blocking stub'):** endpoint guns/masts sited only on safe leaves (never the strike tip, the growing tip, or an island coast link; junction stubs preferred, radius 10→14); no stub near the front → bind a T piece at the tip and arm the farther nub IN THE SAME PASS (traced: 8s-later follow-ups found the fork already shot away at contested fronts — 0 guns built across 2 seeds), nearer nub stays open as strike water. Build-time census: 3/1/2 builds, 100% on junction stubs, zero corking; battery green; Poseidon visibly healthier (6 temples on reference seed) | `56dfc15` | feat: T-fork stub siting |
| 65 | **Fun sweeps (player: 'config option sweeps... starting currency, etc... optimize the game for funness'):** 4 new harnesses, ~120 scored matches. APPLIED: bounty x2 (4/6/10 - neutral on 2 seeds, rescues the third: kill favor rebuilds the net 36→63 segs); wind law tightened (MIN_FRAC 0.28→0.4, BOUND_FLOOR 0.4→0.25, avg 116 vs 106 - the wind should bite; heavy floors flatten it to 88). CONFIRMED UNCHANGED by data: start 100/100 exact grid optimum (70-supply craters to ~70; richer starts plateau lower - more money isn't more fun); mining 2/s; dwell 4s; AI decide 2s (sharply peaked). SKIPPED honestly: powers pricing + salvage (script never exercises them - would score noise). Battery green | `b6ca183` | tune: fun-sweep batch |
| 66 | **Every match ends (self-directed funness audit; census showed 4/8 matches fizzling into full-HP stalemate timeouts):** wrath tides past the ninth (30s cadence, +0.15/tide to 1.8 cap - 0.06/40s was shrugged off); survive nine tides + five wrath tides and ZEUS CALLS THE MATTER SETTLED (arbitration victory, themed both ways; first cut required zero live craft - never true, loiterers persist - weathering IS the verdict). 8-seed/15-min census: 0/8 timeouts. Wind law made legible (tickers on first wait/tack + identify chips). Trial player now plays branchers/hubs, casts both powers, buys the refit - powers finally sweepable honestly (test/opt_powers.js running). Verified by construction: hand slots guarantee extender+brancher+hub - loops never luck-gated | `82478f5` | feat: wrath tides + arbitration |
| 67 | **Powers + refit sweep verdicts (first honest run - trial player now casts/buys):** Tailwind x Wall favor grid is jagged noise across 2-4 (61-72) with one real signal - 5+ favor tailwind uniformly worse (66/62/61 row); hydrogen refit price sweeps FLAT 68/68/68 from 15+4 to 40+10, so price is not the lever (the wind exemption is the value). Current values (3/4, 25+6) stand, config annotated | `7be092a` | docs: sweep-verdict annotations |
| 68 | **Media refresh (player: 'update the readme and screenshots... video of best match seed/gif' → 'a whole new set'):** all 16 screenshots retaken on the current build (capture driver now ends the guided tut + suppresses the lane-law pill — two shots were photobombed); 3 new feature shots (wrath countdown, Zeus verdict, fleet-waits ticker); new film on seed optc with the sweep-scored player — first take (w9iwff) diverged in-browser to a wave-11 loss, second (optc, uncapped) won by arbitration but with 508 segments of favor-overflow road spam on camera, final take caps expansion at 140 segs: 14 waves, both networks ground to stubs (27 vs 22), temple holds, ZEUS CALLS THE MATTER SETTLED. README: new §15 'Every match ends', §12/§14 rewritten | `a8adb22` | docs: full media refresh |
| 69 | **Release zip (player-directed) + CONFIRM legibility (player: 'what does confirm 0.57 mean'):** WINDWARD_submission.zip 0.50 MB — index.html top-level, vendor/, design-intent.docx (refreshed to 494 words: wind law + guaranteed verdict replace stale nine-and-done claims), buildlog.md (renamed per rules). Formal offline test on a FRESH extraction: file:// load, zero non-file requests, no page errors, one real minute of play. Anonymity scans clean (no names/URLs anywhere; title WINDWARD). The ghost's CONFIRM now reads 'CONFIRM · HEADWIND ×0.57' / FAIR WIND / TAILWIND, banded by the wind-law closure threshold — the number was the §21A.5 wind multiplier, now it says so | `4eb4989` | feat: release zip, CONFIRM wording |
